#!/usr/bin/perl

scalar {
  ALSI_VERSION         => "0.4.9",
  COLORS_FILE          => "$ENV{HOME}/.config/alsi/alsi.colors",
  DE_FILE              => "$ENV{HOME}/.config/alsi/alsi.de",
  DEFAULT_COLOR_BOLD   => "black",
  DEFAULT_COLOR_NORMAL => "blue",
  DF_COMMAND           => "/usr/bin/df -Th -x sys -x tmpfs -x devtmpfs -x overlay",
  GTK2_RC_FILE         => "$ENV{HOME}/.gtkrc-2.0",
  GTK3_RC_FILE         => "$ENV{HOME}/.config/gtk-3.0/settings.ini",
  LOGO_FILE            => "$ENV{HOME}/.config/alsi/alsi.logo",
  OUTPUT_FILE          => "$ENV{HOME}/.config/alsi/alsi.output",
  PACKAGES_PATH        => "/var/lib/pacman/local/",
  PS_COMMAND           => "/usr/bin/ps xc",
  SCREENSHOT_COMMAND   => "/usr/bin/scrot -cd 5",
  USAGE_COLORS         => 0,
  USAGE_COLORS_BOLD    => 1,
  USAGE_PRECENT_GREEN  => 50,
  USAGE_PRECENT_RED    => 100,
  USAGE_PRECENT_YELLOW => 85,
  USE_LOGO_FROM_FILE   => 0,
  USE_VALUES_COLOR     => 1,
  WM_FILE              => "$ENV{HOME}/.config/alsi/alsi.wm",
}
